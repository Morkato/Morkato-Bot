import { INTERNALS } from "next/dist/server/web/spec-extension/request"
import { NextRequest, NextResponse } from "next/server"

export async function GET(req: NextRequest, ctx: { params: Record<string, unknown> }) {
  
}