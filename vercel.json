{
  "version": 2,
  "builds": [
    { "src": "package.json", "use": "@vercel/static-build" },
    { "src": "prisma/schema.prisma", "use": "prisma generate" },
    { "src": "src/**", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/$1" }
  ]
}
